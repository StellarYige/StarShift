# 变更记录

## 未发布

- 五工具的队列重试、批量操作、预览及处理改进见[五工具完善记录](docs/FIVE_TOOLS_REPORT.md)。结构整理拆出文件队列、页面编辑和 Worker 通信类型，合并重复采集脚本与发布说明；测量产物默认写入忽略目录，历史证据保留。转换行为与 DOCX 启动策略不变。

- DOCX 启动新增资源完成、WASM 运行时、Worker 加载和文档服务握手的轻量诊断；约 30 秒没有新进展时提示可等待或取消后重新初始化，并区分近期仍收到下载数据与下载进度未知。
- 启动超时说明最后阶段，不再统一归因于文档大小；保留输入、设置和显式重试。原 240 秒启动上限、串行启动及每批释放策略保持不变，偶发停顿根因仍未定位。

证据、针对性验证和限制见 [DOCX 初始化等待修订](docs/DOCX_STARTUP_STALL.md)。本轮不增加版本号或创建 Release。

## 0.1.1

- 停止制作项目静态 ZIP 和校验附件，移除发布打包脚本、npm 命令和 Actions 项目包步骤；部署改为从源码构建。保留 Pages 产物、版本记录、许可和工具中的转换结果 ZIP 下载。
- DOCX 显示检查、加载、初始化、排版和导出阶段，可靠时显示字体读取字节数；保留文件与页数进度语义。下载失败、初始化失败、不兼容、文档错误和超时分别提示。
- 引擎级失败停止当前批次的重复初始化，保留输入供显式重试；单份文档错误可继续处理后续文件。同批串行复用，每批结束释放引擎，不增加持久引擎或字体缓存。
- 修复损坏 PDF 的 Worker 清理、PDF 任务取消、预览错误旧状态、重复 ZIP 的 URL / Worker 增长和取消后的异步状态回写；保留 ZIP 准备完成后的直接下载链接。
- 为缺少 Worker Canvas 的浏览器提供本地 DOM Canvas 图片回退，保留 JPG / PNG / WebP、方向、透明度和图片 PDF 能力；补齐窄屏预览与下载按钮的可访问名称。
- 修复首次绘制与 hash 监听之间的工具切换竞争，以及横屏预览底部操作被裁切的问题。
- 计时器跟踪保留浏览器原有的回调上下文；新增实际线程初始化、十轮生命周期、DOM 编码取消和脚本下载失败恢复验证。
- 为修改的文档适配脚本增加版本标识，固定引擎与字体 URL 不变。
- 清空时卸载持有文件的工作区，释放上次渲染保留的 File / Blob，保留转换设置；使用弱引用和页面 GC 验证。对已实测不兼容的引擎能力提前检查，避免 Firefox / WebKit 反复尝试时先分配大量 WASM 内存。
- Actions 迁移至已核对的 Node 24 运行时版本并固定 SHA；项目 Node 24 和 npm 依赖版本不变。

实测数据、兼容范围、未测项目与发布状态见 [v0.1.1 修订报告](docs/V0.1.1_REPORT.md)。

## 0.1.0

首发五项浏览器本地文件转换工具，历史证据见 [首版测试记录](docs/TEST_REPORT.md)。该版本当时的项目包已停止提供；标签、Release 版本记录和测试事实保留。
